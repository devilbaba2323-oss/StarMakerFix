name: Build Tweak
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Tweak
        uses: theos-dev/theos-action@v1
        with:
          args: make package
